# Example Storybook React Project

This directory is a placeholder for an example Storybook React project.

To create a working example:

1. Create a new React app with Storybook:
   ```bash
   npx create-react-app storybook-example
   cd storybook-example
   npx storybook@latest init
   ```

2. Add Pixelguard:
   ```bash
   npm install pixelguard playwright
   npx playwright install chromium
   ```

3. Start Storybook:
   ```bash
   npm run storybook
   ```

4. Initialize and run tests:
   ```bash
   npx pixelguard init
   npx pixelguard test --update
   npx pixelguard test
   ```
